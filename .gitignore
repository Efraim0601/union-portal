# Dépendances & sorties de build
/node_modules
node_modules/
/dist
/out-tsc
/.angular
/tmp
/bazel-out

# server/diaspora-ocr (Python)
.venv/
__pycache__/
*.pyc

# server/diaspora-otp : base SQLite + documents/pièces d'identité uploadés (données
# personnelles réelles une fois testé en conditions réelles — jamais dans git).
server/diaspora-otp/data/

# Dépôts backend clonés à la racine pour le dev local (projets git séparés,
# NE PAS committer dans union-portal). Voir docker-compose.local.yml dans chacun.
/promoteApp/
/diaspora-onboarding/

# Tests / couverture
/coverage

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environnement / secrets (ne jamais committer)
.env
.env.*
!.env.example

# Éditeurs / OS
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
*.swp

# Modèle OCR latin (téléchargé sur le serveur, non versionné ici)
server/diaspora-ocr/ocr_models/
